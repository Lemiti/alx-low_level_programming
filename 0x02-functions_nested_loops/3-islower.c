#include "main.h"

/**
 * _islower - checks whether character is lower or not
 * Return:1 if the character is in lowercase
 * Return:0 if the character is in not lowercase
 */

int _islower(int c)
{
	
}
